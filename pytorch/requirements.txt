# PyTorch 章节运行环境
# 建议 Python 3.11/3.12（64-bit）；先更新与解释器兼容的 pip
# CPU 安装：pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# GPU 安装：按显卡驱动与所选 PyTorch 构建的兼容条件选择，参见 https://pytorch.org/get-started/

torch>=2.7
torchvision>=0.22

# 数据处理 / 绘图
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3       # iris 数据集（chap4 下）
networkx>=3.1           # chap9 图结构可视化
torch-geometric>=2.6    # chap9 Cora / PROTEINS 工程版实验

# Notebook 执行 / 测试
jupyter>=1.0
nbconvert>=7.0
pytest>=7.0
